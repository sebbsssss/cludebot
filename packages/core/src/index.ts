// Barrel exports for @clude/core
// Consumers should prefer subpath imports (e.g. @clude/core/config) for tree-shaking,
// but commonly-used items are re-exported here for convenience.

export { config } from './config';
